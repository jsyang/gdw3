# YAML equivalent of grunt.registerTask(<taskname>, [<task1>, <task2>, ...])

deploy:
  - build
  - uglify
  - htmlmin
  - inline

build:
  - exec:copyAssets
  - browserify

default:
  - build
  - http-server
  - watch
